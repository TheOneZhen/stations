{
  "name": "stations",
  "version": "1.0.0",
  "description": "Some demos of zhenisbusy-site, can runned in other env on one's own.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "node scripts/publish-with-npm.mjs",
    "build:stations": "pnpm --filter \"./packages/**\" --if-present run build:station"
  },
  "keywords": [
    "demo",
    "front-end"
  ],
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "author": "Z",
  "license": "ISC",
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@changesets/cli": "^2.26.2",
    "@types/node": "catalog:",
    "esbuild": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-format": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
