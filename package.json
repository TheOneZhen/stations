{
  "name": "stations",
  "version": "1.0.0",
  "description": "Some demos of zhenisbusy-site, can runned in other env on one's own.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "demo",
    "front-end"
  ],
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "author": "Z",
  "license": "ISC",
  "dependencies": {
    "pnpm": "^8.8.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "typescript": "^5.0.2"
  }
}
